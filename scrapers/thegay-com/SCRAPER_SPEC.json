{
  "site_name": "TheGay.com",
  "site_url": "https://www.thegay.com/",
  "folder_name": "thegay-com",
  "platform": "TXXX network (custom Vue SPA — same codebase as HClips, HDZog, HotMovs, Upornia, TXXX)",
  "supported_hooks": [
    "sceneByURL"
  ],
  "unsupported_hooks": [
    "sceneByName",
    "sceneByQueryFragment",
    "sceneByFragment",
    "performerByURL"
  ],
  "url_patterns": {
    "scene": [
      "https://www.thegay.com/video/{id}/{slug}/"
    ],
    "performer": [
      "https://www.thegay.com/pornstar/{slug}/  -- NOT scrapeable headlessly (SPA, no SSR metadata)"
    ],
    "search": [
      "https://www.thegay.com/search/{query}/  -- SPA route only, no usable server-rendered data"
    ]
  },
  "platform_notes": {
    "rendering": "Pure Vue SPA. The server returns an empty HTML shell for every route — <title> is blank, no og: meta tags, no JSON-LD. All page data is injected by JS after load.",
    "canonical_pattern": "The <link rel=canonical> in the shell uses the path /api/v2/videos/{id}/ — this is a Vue router path, NOT a REST endpoint. Requesting it directly returns the same empty HTML shell.",
    "known_api": "The TXXX network exposes an internal JSON API used by the Vue app. The live frontend bundle currently calls: GET /api/json/video/86400/{million_bucket}/{thousand_bucket}/{id}.json with a Referer header set to the video page URL. For video 748987, the confirmed path is /api/json/video/86400/0/748000/748987.json.",
    "id_extraction": "The video ID is the first numeric segment in the URL path: /video/{id}/{slug}/ — e.g. /video/748987/bareback-gay-sex-with-jerk/ gives id=748987.",
    "age_gate": "An age_verified=1 cookie is required. Without it the server may redirect to an age-gate page.",
    "anti_bot": "No Cloudflare. A realistic Firefox UA + Accept headers + age_verified=1 cookie is sufficient to fetch the HTML shell. However, the shell contains no metadata — the API call is required."
  },
  "api_strategy": {
    "primary": "GET https://www.thegay.com/api/json/video/86400/{million_bucket}/{thousand_bucket}/{id}.json with headers: User-Agent (Firefox), Referer: https://www.thegay.com/video/{id}/{slug}/, Cookie: age_verified=1, Accept: application/json",
    "fallback": "The bundle also exposes /api/videofile.php for stream file data, but scene metadata comes from the bucketed /api/json/video/... endpoint. Return partial data instead of guessing alternate metadata routes.",
    "id_from_url": "re.search(r'/video/(\\d+)/', url).group(1)",
    "bucket_calculation": {
      "million_bucket": "(int(id) // 1000000) * 1000000",
      "thousand_bucket": "(int(id) // 1000) * 1000",
      "lifetime": 86400
    }
  },
  "metadata_mapping": {
    "scene_title": "API response: data.title or data.name",
    "date": "API response: data.post_date or data.added (ISO-8601, strip to YYYY-MM-DD)",
    "description": "API response: data.description or data.text (may be tag list, not prose)",
    "cover_image": "API response: data.thumbsrc or data.thumb; fallback to data.thumbs[0].src, data.thumb_url, or data.preview_url",
    "performers": "API response: data.models[], data.pornstars[], or data.models_suggested[] when present; many scenes return no performer names",
    "tags": "API response: data.categories object values -> each item has .title; fallback to data.tags[]",
    "studio": "Constant 'TheGay.com' (no per-channel metadata in API)"
  },
  "selectors_or_parsing_notes": {
    "scene_title": [
      "API JSON: response['title'] or response['name']",
      "Fallback (HTML shell): no og:title present — do not rely on HTML"
    ],
    "date": [
      "API JSON: response['post_date'] or response['added']",
      "Strip to YYYY-MM-DD using split('T')[0] or datetime.fromisoformat()"
    ],
    "description": [
      "API JSON: response['description'] or response['text']",
      "May be a comma-separated tag list rather than a narrative synopsis on user-uploaded clips"
    ],
    "cover_image": [
      "API JSON: response['thumbsrc'] or response['thumb']",
      "Fallback: response['thumbs'][0]['src'], response['thumb_url'], or response['preview_url']"
    ],
    "performers": [
      "API JSON: response['models'], response['pornstars'], or response['models_suggested']",
      "Extract title, name, or username when present",
      "Many clips expose no performers at all; return the rest of the scene data"
    ],
    "tags": [
      "API JSON: response['categories'] is often a keyed object rather than a list",
      "Extract title string from each object value; fallback to response['tags']"
    ],
    "studio": [
      "Hard-code to 'TheGay.com' — no per-scene studio field in API"
    ]
  },
  "search_strategy": {
    "supported": false,
    "notes": "All search routes are Vue SPA paths. The server returns an empty HTML shell — there is no server-rendered list of results to parse. sceneByName, sceneByQueryFragment, and sceneByFragment are not feasible without a JS-capable headless browser, which would introduce heavy dependencies. Recommend leaving these unsupported until a lightweight workaround is found."
  },
  "fragment_matching_strategy": {
    "supported": false,
    "notes": "Depends on search, which is not accessible to script scrapers. Not practical."
  },
  "anti_bot_or_access_notes": [
    "No Cloudflare protection detected on the site.",
    "Age gate cookie required: set Cookie: age_verified=1 on all requests.",
    "A realistic Firefox/Linux UA and Accept/Accept-Language headers are sufficient.",
    "The HTML shell returned for all routes contains no useful metadata — all data comes from the internal JSON API.",
    "The video ID is always present in the URL path and must be extracted to call the API."
  ],
  "known_limitations": [
    "sceneByName, sceneByQueryFragment, and sceneByFragment are not supported — search routes return empty SPA shells.",
    "performerByURL is not supported — performer pages are SPA routes with no server-rendered data.",
    "Performer names are often absent from the live metadata payload even when the clip clearly has multiple performers.",
    "The description field may contain a tag list rather than a prose synopsis on user-uploaded clips.",
    "Studio is always hard-coded to 'TheGay.com' — no per-scene channel/studio field available.",
    "Thumbnail CDN URL structure includes a date-path segment that is not reconstructable without the API response."
  ],
  "validation_examples": {
    "scene_urls": [
      "https://www.thegay.com/video/748987/bareback-gay-sex-with-jerk/"
    ],
    "performer_urls": [],
    "search_queries": [],
    "fragments": []
  },
  "sources": [
    "https://www.thegay.com/",
    "https://www.thegay.com/video/748987/bareback-gay-sex-with-jerk/",
    "https://github.com/stashapp/CommunityScrapers",
    "https://github.com/stashapp/stash"
  ]
}
